{
  "name": "pi-loop-guard",
  "version": "1.0.0",
  "description": "Detects and blocks repetitive tool calls within a single turn",
  "keywords": [
    "pi-package"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/eymar/pi-tools-loop-guard.git"
  },
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },

  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  },

  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
